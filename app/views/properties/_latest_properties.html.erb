<% @properties.each do |property| %>
<div class="col-3">
	<%= image_tag property.photo_url, class:'photo-thumbnail' %>
	<h3 class="mt-3"><%= property.name %></h3>
	<p><%= property.address %></p>
</div>
<% end %>